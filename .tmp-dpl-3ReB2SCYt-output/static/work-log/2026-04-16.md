## Completed changes today
- Expanded the auth and admin surface across `api/auth/[action].js`, `api/admin/[...action].js`, `site-auth.js`, `login.html`, `register.html`, `reset-password.html`, and `auth-callback.html` to support the updated login and account flows.
- Refactored transport/public request handling and admin management in `transport-public.js`, `transport-admin.js`, `transport-api.js`, `transport-shared.js`, and the related `api/transport-*` endpoints.
- Updated the pickup/service-center/storage booking screens and shared styling in `pickup.html`, `pickup-form.html`, `pickup-form.js`, `service-center.html`, `storage-booking.html`, `script.js`, and `styles.css`.
- Added and adjusted SQL and backend helpers for order, transport, and user flows in `supabase/*.sql` and `api/_lib/*.js`.
- Removed obsolete public API entry points under `api/public/` and deleted unused hero image assets.

## Files touched
- Auth and admin: `api/auth/[action].js`, `api/admin/[...action].js`, `admin-api.js`, `admin-pages.js`, `admin-shell.js`, `admin-users.html`, `admin-storage.html`, `login.html`, `auth-callback.html`, `site-auth.js`, `google-auth.js`
- Transport and order APIs: `api/_lib/transport.js`, `api/_lib/user-auth.js`, `api/_lib/email-login.js`, `api/_lib/order-numbers.js`, `api/_lib/storage-orders.js`, `api/_lib/http.js`, `api/transport-groups/index.js`, `api/transport-groups/[id].js`, `api/transport-groups/[id]/members.js`, `api/transport-group-members/[id].js`, `api/transport-requests/index.js`, `api/transport-requests/[id].js`
- Public UI and shared frontend: `transport-public.js`, `transport-admin.js`, `transport-api.js`, `transport-shared.js`, `transport-board.html`, `transport-admin-groups.html`, `transport-admin-group-edit.html`, `transport-admin-requests.html`, `transport-admin-request-edit.html`, `transport-admin-request-new.html`, `pickup.html`, `pickup-form.html`, `pickup-form.js`, `service-center.html`, `storage-booking.html`, `script.js`, `styles.css`, `styles-pickup-backup.css`
- Config and data: `.env.example`, `.gitignore`, `package.json`, `package-lock.json`, `dev-server.js`, `supabase/transport_dispatch.sql`
- Removed assets and obsolete endpoints: `api/public/auth-config.js`, `api/public/storage-order-submit.js`, `api/public/transport-groups.js`, `api/public/transport-request-submit.js`, `img/hero-consultation-generated.jpg`, `img/hero-consultation-horde.png`

## Key decisions
- Consolidated public transport and order entry points into the newer shared API structure instead of keeping the old `api/public/*` handlers alive in parallel.
- Kept the auth and admin changes aligned across server handlers and client pages so the login, callback, and admin shells stay on the same flow.
- Treated the transport dispatch SQL and helper library updates as the source of truth for the new request, group, and order behavior.

## Unresolved issues
- `git status` still shows a large set of uncommitted changes, so the current state needs review before release.
- Temporary verify logs remain in the workspace (`.tmp-dev*.log`, `.tmp-vercel*.log`) and should be cleaned up or ignored explicitly.
- The new/updated flows were not re-verified in-browser during this run.

## Tomorrow priorities
- Run targeted verification on auth, admin transport group management, and transport request submission.
- Review the remaining untracked files and decide which belong in source control versus local scratch.
- Confirm the SQL migrations and backend helpers match the frontend request/status workflow end to end.

## Verification methods
- `git status --short`
- `git diff --stat`
- `git diff --name-only`
- No application test or browser verification commands were run during this pass.
