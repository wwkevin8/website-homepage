# Current Status

## Latest Task Update

- Date: 2026-07-08
- Scope: Added a lightweight public pickup/carpool submit loading interaction. This touched only `pickup-form.js`, `transport-public.js`, `styles.css`, and this status note. No database schema, API route, backend/admin behavior, email behavior, submit payload shape, deployment config, GitHub push, or production deployment was changed.
- Summary: The public pickup form submit button now enters an in-flight state after validation and before the pre-submit/API work, showing "提交中..." with a small CSS car-start animation, disabling the button, setting `aria-busy`, preserving the button width during loading, and ignoring repeated submit attempts. The public join-carpool modal now uses the same lightweight car loading treatment for "确认加入拼车" as "加入中..." and for "检查是否可加入" as "检查中..."; preview and submit each have their own in-flight guard. Successful join still replaces the modal with the existing success result, while failure restores the submit button to "确认加入拼车".
- Verification: `node --check pickup-form.js`, `node --check transport-public.js`, and `node scripts/regression-check.js` passed. Headless browser verification with mocked public APIs confirmed the pickup submit button immediately shows loading, includes the car loader, is disabled, sets `aria-busy`, and repeated clicks produce only one submit call. Mocked join-carpool browser verification confirmed the preview button shows loading and blocks repeat checks, the join submit button shows "加入中..." with the car loader and blocks repeat submits, success renders the existing success result, and failure restores the join submit button to an enabled "确认加入拼车" state.
- Open risks / follow-up: Verification used mocked API responses to avoid writing test data. A quick manual phone-width visual check before release is still recommended to confirm the new inline car icon spacing feels right with the live page typography.

- Date: 2026-07-08
- Scope: Fixed the public frontend transport submission summary timezone conversion bug and included the related UK-time form warning already prepared in `pickup-form.html`. This touched only `pickup-form.html`, `pickup-form.js`, `transport-public.js`, `scripts/regression-check.js`, and this status note. No database schema, API save logic, admin display, existing order data, email template/function, deployment config, GitHub push, or production deployment was changed.
- Summary: The customer-reported wrong "拼车表单摘要" was confirmed to come from `pickup-form.js` `buildSummary()`, whose flight and preferred time lines previously used `formatDateTime()` and therefore ran raw `datetime-local` values through `new Date(...)` plus `timeZone: "Europe/London"`. Public submission summaries now format raw `datetime-local` text directly, so `2026-09-18T19:30` displays as `2026/09/18 19:30` and `2026-09-18T21:00` displays as `2026/09/18 21:00` without timezone conversion. The public join-carpool success summary in `transport-public.js` received the same raw-text summary treatment, and the public form warning now explicitly tells customers to fill UK local time rather than China time.
- Verification: `node --check pickup-form.js`, `node --check transport-public.js`, and `node scripts/regression-check.js` passed. The regression check now guards the pickup summary and join summary against reintroducing shared/timezone formatting for raw `datetime-local` summary fields.
- Open risks / follow-up: `api/_lib/transport-order-submission-email.js` still formats saved server timestamps with `Europe/London`, but it was intentionally left unchanged because the screenshot text is not generated by that email function and the saved/admin display path is currently correct. If the product later requires emails to echo the exact pre-save browser input text, that should be handled as a separate API/data-contract change.

- Date: 2026-07-08
- Scope: Adjusted only the public pickup/dropoff form UK-time notice presentation in `pickup-form.html`, plus this status note. No database schema, API, admin logic, JavaScript submit/validation logic, existing order data, automatic timezone conversion, email behavior, deployment config, GitHub push, or production deployment was changed.
- Summary: The existing UK-time warning near the flight/service time fields is now red, slightly bolder, moved upward, and given bottom spacing so it no longer visually crowds the following "同行人数" block.
- Verification: Static inspection confirmed the change is limited to the existing `.carpool-time-hint` paragraph style and text position; the time field names and input types remain unchanged.
- Open risks / follow-up: Browser visual verification on desktop/mobile is still recommended before release because this is a spacing/color polish change based on a screenshot.

- Date: 2026-07-08
- Scope: Implemented the minimal public pickup/dropoff form timezone copy update. This changed only the existing frontend hint text on `pickup-form.html` plus this status note. No database schema, API, admin logic, existing order data, email behavior, automatic timezone conversion, build output, deployment config, GitHub push, or production deployment was changed.
- Summary: The public form's existing `.carpool-time-hint` under the `flight_datetime`, `preferred_time`, and `deadline_date` fields now clearly tells customers to enter UK local time and not China time: "时间请统一填写英国当地时间。系统默认按英国时间保存和显示，请不要填写中国时间；如填写中国时间可能会导致接送机安排错误。" The existing field names, input types, submit payload, validation, summary generation, and styling classes were left unchanged.
- Verification: Static inspection confirmed `flight_datetime`, `preferred_time`, and `deadline_date` remain present with their original input types and that only the hint text changed in `pickup-form.html`. `node --check pickup-form.js` passed.
- Open risks / follow-up: Browser visual verification was not run in this shell; before release, briefly check the public pickup form on desktop and mobile to confirm the longer hint wraps cleanly and remains visible near the time fields.

- Date: 2026-07-08
- Scope: Investigated pickup/dropoff flight time and service time timezone handling only. No source code, API behavior, database schema, email behavior, deployment config, build output, GitHub state, or production data was changed.
- Summary: The public pickup form sends `flight_datetime` and `preferred_time_start` as raw `datetime-local` strings, but its on-page summary formats those raw values through `new Date(...)` plus `timeZone: "Europe/London"`, which can visibly convert a China-browser local input before submission. The server normalizes `datetime-local` strings in `api/_lib/transport.js` as `Europe/London` wall time and saves ISO UTC strings into `timestamptz` columns. Admin list/detail/group/change views then display stored instants with `Intl.DateTimeFormat(..., timeZone: "Europe/London")`. Therefore service time is consistently managed as UK local time, while flight time currently has no preserved "customer-entered local flight time" or explicit timezone marker; any timezone-bearing ISO input, frontend `toISOString()` conversion, or browser-side summary formatting can make a China-local flight time appear as a UK-shifted time.
- Key files/fields: `pickup-form.js` (`flight_datetime`, `preferred_time`/`preferred_time_start`), `api/_lib/transport.js` (`parseLocalTransportDateTime`, `ensureDateTime`, `mapRequestPayload`), `supabase/transport_dispatch.sql` (`transport_requests.flight_datetime`, `transport_requests.preferred_time_start`, `transport_groups.flight_time_reference`, `transport_groups.preferred_time_start` as `timestamptz`), `apps/admin-vue/src/views/TransportRequestsView.vue`, `apps/admin-vue/src/views/TransportRequestDetailView.vue`, and `apps/admin-vue/src/components/TransportOrderChangeDrawer.vue`.
- Verification: Source inspection only; no production Supabase row sampling was performed. The likely fix is to keep pickup/dropoff service time in UK time, but add an explicit timezone policy for flight time: either require/label UK time, or add additive raw/local flight-time fields such as customer-entered local datetime plus timezone/airport-timezone metadata and show both values in admin.
- Open risks / follow-up: Confirm desired product behavior for flight time before code changes. If preserving original customer flight-local time is accepted, implement additively with regression checks and avoid changing existing UK service-time grouping semantics in the same release.

- Date: 2026-06-06
- Scope: Published a second narrowly scoped production hotfix for the public carpool join modal submit experience. The release commit changes only `transport-public.js`; no backend, database, email, deployment config, postage, driver, Admin UI, Supabase SQL, or unrelated dirty work was included.
- Summary: The public "确认加入拼车" submit button now immediately enters a loading state with the text "提交中，请稍候...", becomes disabled, shows "正在提交，请勿重复点击。", and uses an in-flight guard so rapid repeated clicks do not send duplicate submit requests. On success the modal moves to the success state; on failure the button returns to "确认加入拼车" and displays the backend error message.
- Release: Commit `24bccbd` (`fix: add loading state for transport join submit`) was pushed to `origin/release/transport-storage-preflight`. Vercel first created preview `https://webside-aknzf21vs-wwkevin8s-projects.vercel.app`, then production deployment `https://webside-mqxzrzmdo-wwkevin8s-projects.vercel.app`, status `READY`.
- Verification: `node --check transport-public.js`, `node --check api/admin/[...action].js`, `node scripts/regression-check.js`, and `npm.cmd --prefix apps/admin-vue run build` passed. The admin build output was restored before commit, so only `transport-public.js` was committed. Live production browser verification on `https://ngn.best` used mocked auth/preview/submit responses to avoid production writes and confirmed active join entries render, `flight_datetime.min/max` remain `null`, the preview area can show "是否可直接加入：可以" plus risk warning, immediate loading/disabled/no-repeat behavior works, rapid triple-click sends only one submit request, success state displays, failure restores the button and shows backend error text, and full/closed/cancelled groups still do not show visible join entries in the active public list.
- Open risks / follow-up: Production write-through was intentionally not exercised by automation to avoid creating test orders. If a real user reports a submit failure, keep the investigation limited to this hotfix path and the existing carpool join flow.

- Date: 2026-06-05
- Scope: Implemented the requested carpool time-difference relaxation without changing database schema, pricing rules, passenger-count logic, email behavior, or deployment. The change touches public carpool join evaluation/submission/display, backend admin transfer validation/preview/confirmation, operation logging metadata, regression guards, and rebuilt local admin static output because the required admin build was run.
- Summary: Public join no longer blocks pickup groups over 4 hours or dropoff groups over 6 hours solely because of time difference; it returns and displays the warning "该订单时间与目标拼车组时间相差较大，请确认是否仍要加入。" while preserving hard blockers for target group status, full/capacity, service type, airport, expired target order, and duplicate active same-type user orders. Adjacent-date cross-midnight joins are allowed only when the actual time difference is still within the original public window. A follow-up frontend fix removed the leftover `datetime-local` `min`/`max` attributes and old 4h/6h range copy from the public join form, so browser native validation no longer blocks large-time-gap joins after the system evaluation allows them. Public large-time-gap joins write an isolated best-effort `admin_operation_logs` entry with order/contact/time-difference/risk-confirmed metadata and never fail the join if logging fails. Backend transfer validation no longer throws solely because the target group is more than 3 hours away; `change-preview` surfaces a risk warning, `change-confirm` still re-runs preview token/source validation and group existence/capacity/status/type/airport checks, and confirmed transfer logs include operator, order time, target group time, time difference, operation type "后台转组", and risk confirmation metadata. Adjacent-date backend candidates are considered only for the narrow cross-midnight window.
- Verification: Passed `node --check` for `api/_lib/transport-join.js`, `public-api-handlers/transport-join-preview.js`, `public-api-handlers/transport-join-submit.js`, `api/_lib/transport-group-lifecycle.js`, `api/transport-requests/[id]/change-preview.js`, `api/transport-requests/[id]/change-confirm.js`, `transport-public.js`, `api/admin/[...action].js`, and `scripts/regression-check.js`. `npm.cmd --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning and rebuilt local admin assets to `admin/assets/index-Doo5a0QK.js` and `admin/assets/index-D7_HjU-a.css`. `node scripts/regression-check.js` passed and now includes guards for relaxed public join time warnings, frontend risk logging, backend transfer time warnings, and no backend hard block on >3 hours. Follow-up static checks confirmed the public join `flight_datetime` input has no time-window `min`/`max`, no old "前后 4/6 小时" hard-range copy remains in that form, and risk warnings are still rendered in Chinese.
- Acceptance check: A no-write half-real verification was run because no Supabase connection variables are available in the shell. Direct calls to the real public join evaluator passed pickup >4h allowed+warning, dropoff >6h allowed+warning, cross-midnight near match allowed+warning, and hard blocks for full/closed/cancelled/type mismatch/airport mismatch/duplicate same-type active order. A mocked Supabase call through the real backend group validation passed backend >3h allowed+warning, 23:30/01:00 cross-midnight allowed+warning, and hard blocks for capacity, closed, cancelled, type mismatch, and airport mismatch. Static UI checks confirmed the public join confirmation area and admin preview risk area show Chinese risk messages, with no new forced checkbox.
- Release: Published as a production hotfix on 2026-06-06. The original local hotfix commit was `513bf6acc672b052507a953d88cbe72e56f438fb`, but `origin/release/transport-storage-preflight` had newer production commits, so the same scoped hotfix source changes were replayed on top of the remote release branch as commit `0aa6cdfe67ed666d7462a33bea20e530ffe35c15` and pushed to `origin/release/transport-storage-preflight`. The production deployment is `READY`: Vercel deployment `dpl_6gQkcdrHoC8xBpfTtLsWg9F4xb2Z`, URL `https://webside-pky1e0mu4-wwkevin8s-projects.vercel.app`, aliased to `https://ngn.best`. The release commit contains only the seven scoped carpool time-gap hotfix files: `api/_lib/transport-group-lifecycle.js`, `api/_lib/transport-join.js`, `api/transport-requests/[id]/change-confirm.js`, `api/transport-requests/[id]/change-preview.js`, `public-api-handlers/transport-join-submit.js`, `scripts/regression-check.js`, and `transport-public.js`; unrelated dirty files were not committed.
- Production verification: Live browser verification confirmed `flight_datetime.min = null`, `flight_datetime.max = null`, and the browser no longer shows "指定的值不得晚于 2026/06/07 13:00" when a 2026-06-07 07:00 London-time dropoff group is filled with 2026-06-07 18:40. The live active public group list loaded normally, and full / closed / cancelled groups did not expose a visible join entry in the active public list.
- Open risks / follow-up: The final submit request was intercepted to avoid writing a production test order. Real authenticated production acceptance is still needed for: clicking "确认加入拼车" after login and confirming the order inserts successfully, the carpool group member is added, passenger counts update correctly, large-time-gap risk logs are inserted, and backend/admin operation records are visible. Wait for a manual logged-in production acceptance pass before treating write-through behavior as fully verified; if it fails, keep investigation scoped to this hotfix and do not expand into postage, driver, Admin UI, `vercel.json`, or Supabase SQL work.

## Previous Task Update

- Date: 2026-06-04
- Scope: Completed the P0 production hotfix for the 2.0 NGN admin transport groups empty-list issue. This used an isolated clean worktree and touched only the admin Vue API client plus rebuilt admin static asset reference. No driver-side work, new feature, refactor, database change, email/payment behavior, public page change, or unrelated local dirty work was released.
- Summary: Commit `b2bf2c3` (`fix(admin): use admin transport groups list API`) was pushed to `origin/codex/hotfix-admin-groups-list` before deployment. `apps/admin-vue/src/api/admin-api.js` now sends only the admin transport groups list request to `/api/admin/transport-groups`; group detail, update, delete, and member-save calls remain on `/api/transport-groups/:id` and `/api/transport-groups/:id/members`. The rebuilt admin app now loads `admin/assets/index-R-pB7aPy.js` from `admin/index.html`. Production Vercel deployment `dpl_8R7jdL6W27NM54ZQCAdMEYJc9bio` completed as `READY`, URL `https://webside-iracylx21-wwkevin8s-projects.vercel.app`, aliased to `https://ngn.best`.
- Verification: `npm.cmd --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node --check api/admin/[...action].js` passed. `node scripts/regression-check.js` passed with only the existing missing optional storage seed/clear script warnings. The deployed admin HTML references `index-R-pB7aPy.js`; that bundle contains `/api/admin/transport-groups`, keeps `/api/transport-groups/` detail/member prefixes, and no longer contains the old list-request literal. Live unauthenticated checks confirmed `/api/admin/transport-groups?...`, `/api/transport-groups/GRP-260511-A7EM`, and `/api/transport-groups/GRP-260511-A7EM/members` return JSON admin-auth responses rather than JS source or HTML. Public `/api/public/transport-groups?limit=5&page=1` still returned active future groups with total 6. A local mocked admin browser check rendered an existing-data-shaped group row and confirmed the list page requested `/api/admin/transport-groups`.
- Open risks / follow-up: A live authenticated admin browser click-through was not performed because no production admin session was available in the shell; the deployed route, bundle, and JSON behavior are verified. Update/delete were intentionally not exercised live to avoid production writes; their source paths were left unchanged and non-write API execution was checked.

- Date: 2026-06-04
- Scope: Completed the requested low-risk overnight admin display/UI regression pass in the order check -> small repair -> test -> report. This touched admin Vue CSS and rebuilt local admin static assets only. No public/student page styling, login behavior, API, database, email, payment/offline-recorded status logic, route structure, order/price/status business logic, deployment, GitHub push, or production data write was performed.
- Summary: Browser checks opened the built admin app locally with read-only mocked data across dashboard, orders, users, memberships, community, managers, transport requests, transport groups, storage all/today/box/collection/return pages, postage orders, order detail, membership detail, community post detail, and storage detail. Lists were already compact and stable. The remaining low-risk issue was on detail pages: mobile detail titles were still 20px and storage/detail edit controls were looser than the list pages. `apps/admin-vue/src/styles.css` now adds mobile-only admin detail overrides: order/membership/community/storage/box/transport detail titles 16px, detail eyebrow/help text 10px, detail forms 10px labels, 11px inputs/selects at 32-38px height, 11px textareas at 56px minimum height, and compact detail form/table padding. Existing table/list/filter/card modes and horizontal-scroll behavior were preserved.
- Verification: `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed. Playwright rechecked mobile 390px admin pages and confirmed no page-level overflow, no sampled text overflow, and admin density scoping remained active only in admin. Key post-fix mobile metrics: Orders title 16px/table 10px; Transport Requests title 16px/table 10px; Storage Orders title 16px/table 9px; Postage Orders title 16px/table 9px; Order/Membership/Community/Storage detail titles 16px; Storage detail inputs 32-38px at 11px. Desktop spot checks kept 26px detail titles and 41-42px desktop detail controls. Public `pickup.html`, `storage.html`, `transport-board.html`, and `login.html` had no admin density scope and no mobile body overflow.
- Open risks / follow-up: The browser check used mocked read-only admin data rather than live authenticated admin records to avoid writes and production/test data access. Postage drawer and membership modal opening were attempted in the automated pass, but the mocked button labels did not reliably trigger those panels; their CSS remains covered by the existing admin mobile density rules, and they should be included in a quick manual live-admin smoke check tomorrow. Storage All Orders desktop remains an intentionally wide workbench/table surface and was not changed.

- Date: 2026-06-04
- Scope: Re-ran the 2.0 NGN admin display-layer regression in the requested order: check first, repair only confirmed UI density issues, then verify. This touched admin Vue CSS and rebuilt local admin static assets only. No public/student page styling, login behavior, API, database, email, payment/offline-recorded status logic, route, deployment, GitHub push, or production data write was performed.
- Summary: The browser check opened the built admin app locally with read-only mocked data for dashboard, orders, users, memberships, community, managers, transport groups, transport requests, storage all/today/box/collection/return pages, postage orders, and public pages. The check found two remaining mobile density outliers: Orders Center and Transport Requests mobile titles were still 20px, and Transport Requests table text was still 12px. `apps/admin-vue/src/styles.css` now tightens those mobile-only admin rules: Orders Center title 16px, filter labels 10px, inputs/buttons 11px, table body 10px, headers/helper/status/action text 9px; Transport Requests title 16px, filter labels 9px, filter controls/actions 10px, table body 10px, headers/badges/action/workbench text 9-10px. Existing table/list/filter modes and horizontal-scroll behavior were preserved; no card conversion was made. The local admin bundle was rebuilt to `admin/assets/index-D3ZUjEZM.js` and `admin/assets/index-Cu2GkUHR.css`.
- Verification: `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed. Playwright opened the checked admin pages at 390px and confirmed mobile body width stayed 390px with no page-level overflow, admin density scope was active only in admin, and no sampled text overflow was detected in table cells, headers, buttons, inputs, selects, or KPI text. Key mobile metrics after repair: dashboard title 18px; Orders Center title 16px/table 10px/filter two columns; Users title 16px/table 10px/filter two columns; Memberships/Community/Managers title 16px/table 9px; Transport Groups title 16px/table 9px; Transport Requests title 16px/table 10px; Storage all/today/box/collection/return titles 16px with storage tables 9px where rendered; Postage Orders title 16px/table 9px with horizontal scroll. Desktop checks for Orders, Transport Requests, Community, and Storage All Orders kept desktop font/layout behavior; public `pickup.html`, `storage.html`, `transport-board.html`, and `login.html` had no admin density class and no mobile body overflow.
- Open risks / follow-up: The check used mocked read-only admin data rather than live authenticated admin records to avoid writes and production/test data access. Storage All Orders desktop remains an intentionally wide workbench/table surface and was not changed because desktop behavior must stay as-is. Continue preserving existing admin display modes unless the user explicitly asks for a card/list/table mode change.

## Previous Task Update

- Date: 2026-06-04
- Scope: Completed a full regression/self-check of the 2.0 NGN admin mobile UI density work and fixed remaining admin mobile display gaps found during that audit. This touched admin Vue CSS and rebuilt local admin static assets only. No public/student page styling, login behavior, API, database, email, payment/offline-recorded status logic, route, deployment, GitHub push, or production data write was performed.
- Summary: Browser-based checks opened the built admin app locally with read-only mocked admin API responses at 390px mobile width and selected 1366px desktop width. The audit covered dashboard, orders, users, memberships, community, managers, transport groups/requests styling rules, storage all/today/box/collection/return orders, postage orders, and public pages. `apps/admin-vue/src/styles.css` now adds the missing compact mobile density rules for community, managers, storage sub-order pages, and postage orders, while preserving existing table/filter modes and horizontal-scroll tables. Dashboard KPI cards remain two columns on mobile but now have enough height for their 10px/18px/8px text stack without text overflow. The local admin bundle was rebuilt to `admin/assets/index-Beyacsqn.js` and `admin/assets/index-BQKMtKJj.css`.
- Verification: `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed. Playwright opened real built admin pages at 390px with read-only mocked data and confirmed the checked backend pages stayed within the 390px body width, retained admin-only density scoping, rendered two-column mobile filters where applicable, and showed compact table fonts: orders 11px with no ellipsis/truncation issues detected, users 10px, memberships/community/managers/storage/postage 9px, storage/postage wide tables via horizontal scroll, and no checked text overflow in rendered rows/buttons/inputs. Public `pickup.html`, `storage.html`, `transport-board.html`, and `login.html` did not receive `.admin-mobile-density` or `body.admin-mobile-density-active` and had no mobile body overflow. Desktop spot checks for dashboard, community, postage orders, and storage all-orders kept the existing desktop font sizes/layout; storage all-orders remains intentionally wide on desktop as before.
- Open risks / follow-up: The browser audit used mocked read-only admin responses rather than live authenticated admin data to avoid writes or production/test data access. Transport groups/requests were additionally verified by scoped CSS/code inspection because the mock data did not fully reproduce their table rows in the real view; prior focused checks for those scoped rules remain applicable. Desktop storage all-orders still has an existing wide workbench/filter/table surface, which was intentionally not changed because this task required desktop to stay as-is. Continue preserving current table/list/filter/card modes unless the user explicitly requests a mode change.

## Previous Task Update

- Date: 2026-06-04
- Scope: Implemented the P0 independent driver daily work-order dispatch system. This touched database schema, admin Vue pages, admin APIs, independent driver web pages, driver APIs, Vercel rewrites, and docs. No email, payment/offline-recorded status logic,鎺ラ€佹満/鎷艰溅 source generation, production deployment, GitHub push, or production data write was performed.
- Summary: Added `supabase/20260604_driver_daily_tasks.sql` with `drivers`, `driver_tasks`, and `driver_task_logs` tables, RLS, direct public access revokes, password-hash-only driver accounts, and soft-delete/disable support. Added `api/_lib/driver-tasks.js`, `/api/admin/drivers`, `/api/admin/driver-tasks`, and independent `/api/driver/*` routes with a separate `ngn_driver_session` cookie. Added Vue admin pages for 鈥滃徃鏈虹鐞嗏€?and 鈥滃綋澶╁伐鍗曡皟搴︹€? including driver create/edit/reset password/disable, storage+postage generate/refresh that only inserts missing tasks, manual temporary tasks, driver assignment, same-driver drag sorting, admin task snapshot edits, and driver feedback visibility. Added independent mobile-friendly driver pages at `/driver/login` and `/driver/tasks`; drivers can only read their own tasks and can update only `driver_note` plus pending/completed status. Added Vercel rewrites for the driver pages and updated `docs/PROJECT_MAP.md`.
- Verification: `node --check api/_lib/driver-tasks.js`, `node --check api/driver/[...action].js`, `node --check driver-auth.js`, and `node --check driver-tasks.js` passed. `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning and rebuilt the local admin bundle to `admin/assets/index-Cyk1KKYF.js` and `admin/assets/index-BC55QLgD.css`. `node scripts/regression-check.js` passed. A local helper server on port 3010 plus Playwright checked `driver-login.html` and `driver-tasks.html` at 390px width: both loaded expected titles/headings/forms/buttons and had no horizontal overflow. The local helper server does not apply the Vercel `/driver/*` rewrite, so the direct `.html` files were checked. Full database write-through flow testing was not run because the current shell has no `SUPABASE_URL` or `LOCAL_SUPABASE_URL`; the new SQL migration must be applied to a local/test Supabase before testing driver creation, task generation, assignment, sorting, login, note, completion, and backend sync end to end.
- Open risks / follow-up: Apply `supabase/20260604_driver_daily_tasks.sql` to the intended local/test Supabase before using the feature. Then run the required local flow test: create driver, generate tasks, add manual task, assign driver, same-driver drag sort, driver login, driver note, mark complete, and verify admin sync. P0 source generation is intentionally limited to storage+postage+manual; airport/rideshare are schema/API extension points only. Existing unrelated dirty files and prior local admin build artifacts were present and were not reverted.

## Previous Task Update

- Date: 2026-06-04
- Scope: Fixed the admin Storage Today Work Orders mobile table density and information display while preserving the existing worksheet table/horizontal-scroll mode. No API, database, email, payment/offline-recorded status logic, route, public page, login-page, deployment, GitHub push, production data write, or test-data import was performed.
- Summary: `apps/admin-vue/src/styles.css` now gives `storage-today-work-orders-view` a tighter scoped mobile override: title 16px, filter label 9px, date input 10px, query button 9px, summary 10px, table headers 8px, table cells 9px, date/order helper text 8px, remark textarea 8px, and row action buttons 8px. The mobile worksheet filter now keeps date and query on one compact row. The daily worksheet table is forced back to real table display, keeps all 13 columns visible through horizontal scroll, uses a 1280px mobile table width with narrower column allocation, wraps address/service/notes cells inside their own columns, and reduces remark textarea height so rows no longer have large empty vertical space. The local admin bundle was rebuilt to `admin/assets/index-BCeuWdKj.js` and `admin/assets/index-B4alRlKV.css`.
- Verification: `npm --prefix apps/admin-vue run build` passed twice with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed twice. A temporary local HTTP verification page loaded the real built admin CSS at 390px width and confirmed the worksheet table renders as `display: table`, body scroll width stayed 390px, table `min-width` is 1280px with horizontal scrolling available, the first viewport reaches the service-item column instead of showing a large blank area, row height is about 69px, remark textarea height is 34px, and no checked filter/summary/table/textarea/button text overflow was detected. The temporary verification page was deleted and the local service was stopped.
- Open risks / follow-up: Browser verification used a static local style fixture rather than real authenticated admin data to avoid login writes or production data access. Existing unrelated dirty files and prior admin build artifacts were present and were not reverted.

## Previous Task Update

- Date: 2026-06-04
- Scope: Tightened the admin Storage All Orders mobile filter, stats, bulk toolbar, table, and pagination density while preserving the existing filter/table/horizontal-scroll mode. No API, database, email, payment/offline-recorded status logic, route, public page, login-page, deployment, GitHub push, production data write, or test-data import was performed.
- Summary: `apps/admin-vue/src/styles.css` now gives `storage-all-orders-view` a scoped mobile density override: title 16px, filter labels 9px, inputs/selects 10px, filter chips/buttons 9px, stats labels 9px, stats values 18px, bulk toolbar text/buttons 9px, table headers 8px, table cells 9px, badges/status toggles/table action buttons 8px, and pagination 9-10px. The all-orders search remains full-width, valid-range and quick-date controls share one row, remaining filters render two per row, query/reset/export actions render three across, stats cards render two per row, bulk toolbar actions render two per row, and the order table keeps table mode with horizontal scroll and wider status columns to avoid overlap. The local admin bundle was rebuilt to `admin/assets/index-yuwjhwvB.js` and `admin/assets/index-DzEb5QV2.css`.
- Verification: `npm --prefix apps/admin-vue run build` passed twice with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed twice. A temporary local HTTP verification page loaded the real built admin CSS at 390px width and confirmed the all-orders filter grid is `173px 173px`, search spans full width, filter actions are three columns, stats are `182px 182px`, bulk toolbar actions are two columns, body scroll width stayed 390px, table `min-width` is 900px with horizontal scrolling available, input/chip/filter-button heights are 28px, stat card height is 54px, and no checked filter/stats/toolbar/table/pagination text overflow was detected. The temporary verification page was deleted and the local service was stopped.
- Open risks / follow-up: Browser verification used a static local style fixture rather than real authenticated admin data to avoid login writes or production data access. Existing unrelated dirty files and prior admin build artifacts were present and were not reverted.

## Previous Task Update

- Date: 2026-06-04
- Scope: Tightened the admin Memberships mobile page, filter panels, tables, action buttons, and pagination typography while preserving the existing page/filter/table mode. No API, database, email, payment/offline-recorded status logic, route, public page, login-page, deployment, GitHub push, production data write, or test-data import was performed.
- Summary: `apps/admin-vue/src/styles.css` now gives the Memberships page a scoped mobile density override following the user's smaller backend mobile standard: page title 16px, top action buttons 9px, section titles 13px, section helper text 9px, filter labels 9px, inputs/selects 10px, filter buttons 9px, table headers 8px, table cells 9px, last-operation text 8px, badges 8px, row action buttons 8px, and pagination 9-10px. Membership and activation-code filters now keep the search field full-width, render subsequent fields two per row, and render action buttons side by side; the existing table/horizontal-scroll mode is preserved and no card conversion was made. The local admin bundle was rebuilt to `admin/assets/index-CgNPVqVQ.js` and `admin/assets/index-EJT5w9yR.css`.
- Verification: `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed. A temporary local HTTP verification page loaded the real built admin CSS at 390px width and confirmed the Memberships filter grid is `163px 163px`, the search field spans full width, later filter rows contain two fields, action buttons are two columns, body scroll width stayed 390px, table `min-width` is 860px with horizontal scrolling available, input/filter/top-action button heights are 28px, and the intended mobile font sizes are applied. The temporary verification page was deleted and the local service was stopped.
- Open risks / follow-up: Browser verification used a static local style fixture rather than real authenticated admin data to avoid login writes or production data access. Existing unrelated dirty files and prior admin build artifacts were present and were not reverted.

## Previous Task Update

- Date: 2026-06-04
- Scope: Tightened the admin Transport Groups mobile filter and table typography while preserving the existing filter/table mode. No API, database, email, payment/offline-recorded status logic, route, public page, login-page, deployment, GitHub push, production data write, or test-data import was performed.
- Summary: `apps/admin-vue/src/styles.css` now gives the Transport Groups page a scoped mobile density override following the user's smaller backend mobile standard: page title 16px, filter labels 9px, filter inputs/selects 10px, filter buttons 9px, toolbar summary 9px, table headers 8px, table cells 9px, helper text 8px, badges 8px, action buttons 8px, pagination 9-10px. The keyword search remains full-width, primary filter fields render two per row, the filter action buttons render three across, and the existing table/horizontal-scroll mode is preserved. The local admin bundle was rebuilt to `admin/assets/index-Dvfmkby6.js` and `admin/assets/index-Ct7Wewk6.css`.
- Verification: `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed. A temporary local HTTP verification page loaded the real built admin CSS at 390px width and confirmed the Transport Groups filter grid is `173px 173px`, keyword search spans full width, subsequent filter rows contain two fields, action buttons are three columns, no checked filter/table/pagination text overflow was detected, no filter field overlap was detected, body scroll width stayed 390px, table `min-width` is 760px with horizontal scrolling available, input/button heights are 28px, table row height is about 46px, and row action button height is 22px. The temporary verification page was deleted and the local service was stopped.
- Open risks / follow-up: Browser verification used a static local style fixture rather than real authenticated admin data to avoid login writes or production data access. Existing unrelated dirty files and prior admin build artifacts were present and were not reverted.

## Previous Task Update

- Date: 2026-06-04
- Scope: Tightened the admin Users Management mobile filter layout and page typography while preserving the existing filter/table mode. No API, database, email, payment/offline-recorded status logic, route, public page, login-page, deployment, GitHub push, production data write, or test-data import was performed.
- Summary: `apps/admin-vue/src/styles.css` now gives the Users Management page a scoped mobile density override: the keyword search remains full-width, login provider and page size render side by side, and search/reset buttons render side by side. The Users page mobile title, filter labels, inputs/selects/buttons, table header/cell text, badges, row action buttons, and pagination are all reduced according to the user's smaller backend mobile typography preference. The existing table/filter mode was not changed. The local admin bundle was rebuilt to `admin/assets/index-CwXsJhjk.js` and `admin/assets/index-Cy5gYV0a.css`.
- Verification: `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed. A temporary local HTTP verification page loaded the real built admin CSS at 390px width and confirmed the Users filter grid is `171.5px 171.5px`, the keyword field spans full width, login provider and page size share one row, search/reset buttons are two columns, no checked filter/table/pagination text overflow was detected, no filter field overlap was detected, body scroll width stayed 390px, table `min-width` is 760px, input and filter button heights are 30px, table row height is 34px, page title is 16px, filter labels are 10px, inputs/selects/buttons are 11px, table headers are 9px, table cells are 10px, badges/action buttons are 9px, pagination text is 10px, and pagination buttons are 11px. The temporary verification page was deleted and the local service was stopped.
- Open risks / follow-up: Browser verification used a static local style fixture rather than real authenticated admin data to avoid login writes or production data access. Existing unrelated dirty files and prior admin build artifacts were present and were not reverted.

## Previous Task Update

- Date: 2026-06-04
- Scope: Removed mobile ellipsis truncation from the admin Orders Center table while preserving the existing table/horizontal-scroll mode. No API, database, email, payment/offline-recorded status logic, route, public page, login-page, deployment, GitHub push, production data write, or test-data import was performed.
- Summary: `apps/admin-vue/src/styles.css` now scopes an Orders Center mobile table override so order numbers, customer/source text, contact details, dates, and other `.cell-truncate` / `.cell-stack small` content no longer use ellipsis truncation on mobile. The Orders Center table is widened to `min-width: 1100px`, uses `width: max-content` and `table-layout: auto`, keeps table cells in normal table mode, and relies on the existing table wrapper horizontal scroll so operators can swipe right to see full information without text overlap. The local admin bundle was rebuilt to `admin/assets/index-DGwSCt2M.js` and `admin/assets/index-DQDLe3xS.css`.
- Verification: `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed. A temporary local HTTP verification page loaded the real built admin CSS at 390px width and confirmed the Orders Center table width is 1100px, wrapper width is 353px, horizontal scrolling is available, table layout is `auto`, no checked `.cell-truncate` / `.cell-stack small` nodes use `text-overflow: ellipsis`, no clipped text was detected, no first-row cell overlap was detected, and document body width stayed 390px. The temporary verification page was deleted and the local service was stopped.
- Open risks / follow-up: Browser verification used a static local style fixture rather than real authenticated admin data to avoid login writes or production data access. Existing unrelated dirty files and prior admin build artifacts were present and were not reverted.

## Previous Task Update

- Date: 2026-06-04
- Scope: Recorded the user's admin mobile typography preference for future backend UI density work. Documentation/status only; no frontend code, API, database, email, payment/offline-recorded status logic, route, public page, login-page, deployment, GitHub push, production data write, or test-data import was performed.
- Summary: Future admin mobile UI adjustments should use the latest compact dashboard scale as the reference, but generally go about 2px smaller when practical: around 16px for main mobile admin titles, 9-10px for compact card/table/filter labels, 18px for key KPI numbers, 8-9px for helper text, 11px for compact panel titles, and 8-9px for dense rows/logs/quick entries. This is a preference standard, not permission to change display modes: preserve existing table/list/filter/card modes unless the user explicitly asks for a mode change, and always verify no text overlap or unusable controls.
- Verification: Confirmed this preference note is stored in `docs/current-status.md`. No build or browser verification was needed because no functional or style code changed.
- Open risks / follow-up: Some 8-9px text can become hard to read on certain devices, so future changes should balance the requested smaller size with no overlap and minimum usable tap/control sizes.

## Previous Task Update

- Date: 2026-06-04
- Scope: Tightened the admin Orders Center mobile filter layout and order table density while preserving the existing filter/table mode. No API, database, email, payment/offline-recorded status logic, route, public page, login-page, deployment, GitHub push, production data write, or test-data import was performed.
- Summary: `apps/admin-vue/src/styles.css` now gives the Orders Center a scoped mobile filter override: the order number search stays full-width, the remaining filter fields render two per row, and the search/reset buttons render side by side. The mobile Orders Center filter labels, inputs, selects, buttons, bulk toolbar, table cells, table headers, cell helper text, badges, checkboxes, and row action buttons are all reduced further for higher information density without changing field order or business behavior. The local admin bundle was rebuilt to `admin/assets/index-D0E2B6NZ.js` and `admin/assets/index-DYQ5Dm0i.css`.
- Verification: `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed. A temporary local HTTP verification page loaded the real built admin CSS at 390px width and confirmed the Orders Center filter grid is `171.5px 171.5px`, the first order-number field spans full width, subsequent filter rows contain two fields each, search/reset buttons are two columns, no checked filter/table text overflow was detected, no filter field overlap was detected, body scroll width stayed 390px, order table `min-width` is 820px, input height is 32px, filter button height is 31px, table row height is 55px, filter labels are 11px, inputs/selects/buttons 12px, bulk buttons 11px, table cells 11px, table headers 10px, cell helper text 10px, badges 10px, and row action buttons 10px. The temporary verification page was deleted and the local service was stopped.
- Open risks / follow-up: Browser verification used a static local style fixture rather than real authenticated admin data to avoid login writes or production data access. Existing unrelated dirty files and prior admin build artifacts were present and were not reverted.

## Previous Task Update

- Date: 2026-06-04
- Scope: Further reduced the admin dashboard mobile typography after the first dashboard density pass. No layout mode change, API, database, email, payment/offline-recorded status logic, route, public page, login-page, deployment, GitHub push, production data write, or test-data import was performed.
- Summary: `apps/admin-vue/src/styles.css` now applies a tighter dashboard-only mobile font scale: dashboard title 18px, hero description and refresh button 12px, KPI labels 11px, KPI numbers 20px, KPI helper text 10px, panel titles 13px, panel descriptions 11px, task/risk/list text 10-11px, risk numbers 18px, quick links 11px, and recent operation log table cells 11px. KPI cards were also shortened to 82px high while preserving the existing two-column, three-row mobile card grid and existing dashboard/card/panel mode. The local admin bundle was rebuilt to `admin/assets/index-D9wL2iQw.js` and `admin/assets/index-Bfw-0iHM.css`.
- Verification: `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed. A temporary local HTTP verification page loaded the real built admin CSS at 390px width and confirmed the KPI grid remains `181px 181px`, six KPI cards render as three rows, card height is 82px, no KPI card overlap was detected, no checked dashboard text overflow was detected, and body scroll width stayed 390px. The temporary verification page was deleted and the local service was stopped.
- Open risks / follow-up: Browser verification used a static local style fixture rather than real authenticated admin data to avoid login writes or production data access. Existing unrelated dirty files and prior admin build artifacts were present and were not reverted.

## Previous Task Update

- Date: 2026-06-04
- Scope: Adjusted the admin dashboard mobile density and KPI grid while preserving the existing dashboard/card/panel mode. No API, database, email, payment/offline-recorded status logic, route, public page, login-page, deployment, GitHub push, production data write, or test-data import was performed.
- Summary: `apps/admin-vue/src/styles.css` now overrides the admin mobile dashboard KPI area to use two columns at phone width, so the six top statistics render as three compact rows instead of one full-width row each. Dashboard KPI cards have smaller mobile icon/title/helper spacing, decorative circles are reduced, and the lower dashboard panels such as today's todos, risk alerts, recent operation logs, and quick links use smaller mobile headings, row text, table cells, buttons/links, and gaps. The existing dashboard layout mode was not changed. The local admin bundle was rebuilt to `admin/assets/index-lPpr9joL.js` and `admin/assets/index-CoeC_57F.css`.
- Verification: `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed. A temporary local HTTP verification page loaded the real built admin CSS at 390px width and confirmed the KPI grid is `181px 181px`, six KPI cards render as three rows, no KPI card overlap was detected, no checked dashboard text overflow was detected, body scroll width stayed 390px, quick links render as two columns, and mobile dashboard font sizes are: hero title 20px, KPI label 12px, KPI number 22px, KPI helper 11px, panel title 15px, panel description 12px, task title 12px, task helper 11px, risk number 20px, quick link 12px, and recent-log table cell 12px. Temporary verification files were deleted and the local service was stopped.
- Open risks / follow-up: Browser verification used a static local style fixture rather than real authenticated admin data to avoid login writes or production data access. Existing unrelated dirty files and prior admin build artifacts were present and were not reverted.

## Previous Task Update

- Date: 2026-06-04
- Scope: Fixed the mobile overlap in the admin storage "褰撳ぉ宸ュ崟" page while preserving the table row/column mode. No API, database, email, payment/offline-recorded status logic, route, public page, login-page, deployment, GitHub push, production data write, or test-data import was performed.
- Summary: The attempted mobile card-style conversion was reverted. `apps/admin-vue/src/views/StorageTodayWorkOrdersView.vue` is back to plain table cells with no mobile `data-label` card labels. `apps/admin-vue/src/styles.css` scopes the fix to `.storage-today-work-orders-view` and keeps mobile as a real table: table header, colgroup, rows, and cells remain table elements; the wrapper scrolls horizontally; the table keeps enough width for all columns; and long address/payment/remark cells wrap inside their own cells instead of overlapping neighboring columns. The earlier local admin bundle for that task was rebuilt to `admin/assets/index-BZ_ZS57N.js` and `admin/assets/index-BhPD5fU7.css`; the current bundle is now `admin/assets/index-lPpr9joL.js` and `admin/assets/index-CoeC_57F.css`.
- Verification: `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed. A temporary local HTTP verification page loaded the real built admin CSS at 390px width and confirmed table mode, horizontal scrolling, visible header/colgroup, `min-width: 1320px`, wrapping long cells, and no overlapping cells.

## Previous Task Update

- Date: 2026-06-03
- Scope: Optimized 2.0 NGN admin mobile content density across the Vue admin main area. No API, database, email, payment/offline-recorded status logic, route, public page, login-page, deployment, GitHub push, production data write, or test-data import was performed.
- Summary: `apps/admin-vue/src/layouts/AdminLayout.vue` marks the backend main content with `admin-mobile-density` and toggles `body.admin-mobile-density-active` while the admin layout is mounted so Teleport modals remain scoped to backend pages. `apps/admin-vue/src/styles.css` adds a mobile-only `max-width: 768px` density layer for admin body content: compact page headings, card/filter/detail padding, table cells, badges, buttons, inputs, modals, and drawer panels. Desktop admin, sidebar behavior, public/student pages, APIs, database, and routes are intentionally untouched.
- Verification: `npm --prefix apps/admin-vue run build` passed with only the existing Vite large-chunk warning. `node scripts/regression-check.js` passed. Static mobile CSS verification confirmed compact typography and control sizing within the admin-scoped density layer.

## Current Project State

- Production is live on `https://ngn.best`; no deployment was performed for the latest local UI changes.
- The current local admin build output points to `admin/assets/index-Doo5a0QK.js` and `admin/assets/index-D7_HjU-a.css`.
- The carpool time-difference hard-limit relaxation is local only until intentionally committed, pushed, and deployed.
- The admin mobile density audit/fixes and the new driver daily dispatch P0 work are local only until intentionally committed, pushed, and deployed in the required release order.
- The driver daily dispatch schema migration exists locally at `supabase/20260604_driver_daily_tasks.sql` and must be applied to local/test Supabase before end-to-end write testing.
- Existing unrelated dirty files and earlier generated admin assets are present in the worktree and were not reverted.

## Open Risks / Follow-Up

- Before any preview or production release, commit and push the intended changes to GitHub first, then run the relevant Vercel build/deploy command.
- Before using the driver daily dispatch feature, apply `supabase/20260604_driver_daily_tasks.sql` to local/test Supabase and run the full driver flow test.
- Browser verification for the latest admin UI tasks used static local fixtures with the real built CSS rather than authenticated live admin data to avoid login writes or production data access.
- Continue preserving existing admin display modes unless the user explicitly asks for a mode change such as converting a table/list into cards.

## Document Rules

- Read this file together with `E:\webside\AGENTS.md` before analysis or implementation.
- Keep this document as the latest handoff snapshot; rewrite outdated sections instead of appending raw session logs.
